package com.zonaut.playground.reactive.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class TraceConfig {

    // TODO Do something with Sleuth or others to add a trace id over all request between services
    //      - also add the trace id to the exception handlers

}
